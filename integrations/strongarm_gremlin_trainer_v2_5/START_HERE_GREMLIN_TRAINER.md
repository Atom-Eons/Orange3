# START HERE — GREMLIN TRAINER V2.5

Validate seed data:

```bash
python scripts/validate_gremlin_dataset.py
```

Split data:

```bash
python scripts/make_train_val_split.py
```

Run eval stub:

```bash
python evals/eval_gremlin_packets.py
```

Then move to Colab:

```text
train_colab/gremlin_unsloth_train.py
```

Core doctrine:

```text
Do not train a pirate chatbot first.
Train a Gremlin packet model first.
Gremlin pressures.
Mirror verifies.
STRONGARM disciplines.
JUDGEMENT decides.
```
