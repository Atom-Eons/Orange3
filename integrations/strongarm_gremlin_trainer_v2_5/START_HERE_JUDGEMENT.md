# START HERE — JUDGEMENT V2.4

King is gone.

The final synthesis node is now:

```text
JUDGEMENT
```

Run:

```bash
python council_v2.py init
python council_v2.py run "test Judgement replacing King" --mode cheap --heuristic
python council_v2.py run "test Judgement replacing King" --mode normal --heuristic
python council_v2.py server
```

Open:

```text
http://127.0.0.1:8095/ui
```

Print Gremlin policy:

```bash
python scripts/print_gremlin_policy.py
```

New doctrine:

```text
Operator rules.
Config declares.
Gremlin sees.
Mirror proves.
STRONGARM disciplines.
JUDGEMENT decides.
Receipts prove.
```
