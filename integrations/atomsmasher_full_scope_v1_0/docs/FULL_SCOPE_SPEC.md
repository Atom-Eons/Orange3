# Full Scope Spec

## Architecture

Raw input / upload / code / data / tool result
→ full ingest + coverage receipt
→ order detection + HOT_ALWAYS heat governance
→ source chunks + FTS index
→ commitment atoms + AIR
→ equation packets + residuals
→ exact/semantic/runtime cache lookup
→ sparse workset
→ least-action route
→ expansion warrant if needed
→ answer / build / act
→ saved-work certificate
→ compression-debt receipt
→ pathwave/proof/learning receipts

## 620-addition execution model

Each addition has:

- id
- name
- category
- engine handler
- default heat class
- executable action
- receipt output

This prevents 620 fake files. The addition registry is active and tested end-to-end.
