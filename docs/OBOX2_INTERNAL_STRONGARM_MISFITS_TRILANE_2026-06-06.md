# OBOX 2 Internal Backend Upgrade

This is an internal Orangebox Ops milestone, not a frontend rename.

## What Changed

- STRONGARM EASY remains the live local pressure gate on `127.0.0.1:8094`.
- Misfits/Gremlin trainer v2.5 is installed under `integrations/strongarm_gremlin_trainer_v2_5`.
- The elite 1,000-row packet dataset v1.3 is installed under `integrations/strongarm_gremlin_elite_1000_v1_3`.
- Tri-lane routing now has visible config:
  - `config/model_registry.json`
  - `config/role_map.json`
  - `config/routing_policy.json`
- The OBOX 2 internal Codexa setup pack is generated with:
  - `npm.cmd run obox2:pack`

## Doctrine

```text
Gremlin pressures.
Mirror verifies.
STRONGARM disciplines.
JUDGEMENT decides.
Operator rules.
Receipts prove.
```

## Wildcard Law

Dolphin and abliterated models are allowed as Gremlin/Misfit pressure lanes.

They may expose weak refusals, fake productivity, scope collapse, and soft corporate fog.

They may not serve as:

- Judgement
- Mirror truth
- final answer authority
- policy decision authority
- operator approval

## Training Status

The elite dataset and trainer are proofed locally, but no LoRA training is claimed yet.

Training needs an explicit runtime choice on Codexa, Colab, or another GPU box.

First recommended base:

```text
Qwen3-4B for schema obedience; compare Dolphin 8B and an abliterated 8B after.
```

## Proof Commands

```powershell
npm.cmd run strongarm:doctor
npm.cmd run gremlin:doctor
npm.cmd run trilane:doctor
npm.cmd run obox2:pack
npm.cmd run backend:proof
npm.cmd run ops:readiness
```

## Codexa Install

The generated setup zip lands at:

```text
C:\Users\a\Downloads\Orangebox_V2_Internal_Setup_Pack.zip
```

Run it on Codexa / AI Box. The cockpit cannot honestly claim those models are installed until Codexa receipts or probes say so.
