# SOUL GENOME Knowledge Engine Map

SOUL GENOME is accepted as an internal Orangebox continuity object.

It is not a rename, not a hidden leader, not a model body, and not proof that training has happened.

## Accepted Ideas

- Weights are replaceable; receipts, data, probes, and lineage survive.
- The ledger is richer than DPO. It can compile into DPO later.
- The operator should see critical continuity failures.
- Identity continuity needs a CI/CD loop: receipts, dataset compiler, probes, candidate sandbox, adapter compiler, continuity score, promotion or rejection.

## Orangebox Mapping

```text
Activation Layer       -> primers, skills, SOUL GENOME config
Memory Layer           -> ChatBackup, AtomSmasher, CLC/void/lattice memory
Behavior Ledger        -> receipts, STRONGARM audits, gauntlet proofs
Preference Data        -> Misfits elite dataset now; DPO compiler later
Eval Probe Battery     -> STRONGARM/Misfits probes now; continuity probes later
Model Router           -> tri-lane registry and role map
Adapter Compiler       -> trainer scripts now; LoRA receipts later
Drift Monitor          -> reality watch and STRONGARM now; baseline diff later
Lineage and Versioning -> source locks and hashes now; SOUL lineage later
Operator Approval      -> Orangebox law now; promote/re-soul commands later
```

## Proof

Run:

```powershell
npm.cmd run soul:doctor
```

The doctor writes:

```text
C:\Users\a\OrangeBox-Data\knowledge\soul-genome\latest-soul-genome-doctor.json
```

## Doctrine

```text
Do not preserve the model.
Preserve the genome.

Do not worship the adapter.
Recompile the adapter.

Do not trust the new body.
Probe it.

Do not guess continuity.
Score it.

Do not let hidden leaders emerge.
Receipt everything.
```
